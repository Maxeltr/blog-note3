<h1>WriteController::editAction()</h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url('editTag', array(), true));
$form->prepare();

$form->get('submit')->setValue('Update Tag');

echo $this->form()->openTag($form);

echo $this->formCollection($form);

echo $this->form()->closeTag();
